<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.daggerhomework">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DHApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".view.NavExampleActivity"
            android:label="@string/title_activity_nav_example"/>
        <activity
            android:name=".view.NavigateActivity"
            android:label="@string/title_activity_navigate"/>
        <activity android:name=".view.BaseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.repo.details.RepoDetailsActivity" />
        <activity android:name=".view.user.UserDetailsActivity" />
        <activity android:name=".view.repo.list.RepoListActivity" />
    </application>

</manifest>